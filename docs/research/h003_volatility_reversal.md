# H003 — انعكاس/انكماش التقلّب المدفوع بـ`NATR_14`

**الحالة: مقبولة (أول فرضية مقبولة رسمياً في تاريخ المشروع بأكمله).** مسجّلة رسمياً في [`hypothesis_h003_volatility_reversal.ipynb`](../../hypothesis_h003_volatility_reversal.ipynb) عبر محور التقييم في [`signal_evaluation_axis (3).ipynb`](../../signal_evaluation_axis%20(3).ipynb).

## كيف ظهرت النتيجة

بعد أن رفضت المراحل ١-٣ كل ما اختُبر منها على الأصول الخمسة الأصلية (راجع [المرحلة ١-٢](discovery_lab_phase1_2.md) و[المرحلة ٣](discovery_lab_phase3.md))، استُغِلّ توفّر مجلد `history_1d` بمئات العملات فعلياً على Drive لبناء عيّنة أوسع وأقلّ ترابطاً:

- **50 أصلاً حقيقياً**: الخمسة الأصليون + `BTCUSDT` + 44 عملة راسخة متنوّعة (ETH، XRP، ADA، DOGE، DOT، LTC، ATOM، AVAX، LINK، AAVE، وغيرها).
- **`market_context.enabled=True`** صراحةً — يُصلح خلل `MKT_beta` الموثَّق في [المرحلة ١-٢](discovery_lab_phase1_2.md#️-خلل-مُكتشَف-mkt_betamarket_beta-لم-يُختبَر-فعلياً-قط) — `BTCUSDT` كعملة مرجعية حقيقية.
- **94,961 عيّنة، 37 ميزة، 30 نافذة متحرّكة** (بدل 12-56 نافذة على 5 أصول فقط سابقاً).

أُعيد اختبار كل المرشّحين القياسيين الـ26 × 3 أهداف (78 تجربة `EXPANDED_*` جديدة).

## النتيجة: مرشّحان مبنيّان على `NATR_14` مقبولان رسمياً

| المرشّح | التعريف | الهدف | mean_ic | std_ic | frac_significant | consistent_sign | n_ok |
|---|---|---|---|---|---|---|---|
| `NATR_neg_vol` | `-NATR_14` | high | **-0.1970** | 0.0969 | **93.3%** | **True (30/30)** | 30 |
| `NATR_neg_vol` | `-NATR_14` | low | **+0.1644** | 0.0688 | **96.7%** | **True (30/30)** | 30 |
| `RSI_vol_adjusted_reversion` | `-(RSI_14-50)/(NATR_14+1e-6)` | high | **-0.1980** | 0.0963 | **93.3%** | **True (30/30)** | 30 |
| `RSI_vol_adjusted_reversion` | `-(RSI_14-50)/(NATR_14+1e-6)` | low | **+0.1667** | 0.0687 | **96.7%** | **True (30/30)** | 30 |

## تحقّق من المتانة

فُحص الجدول الكامل لكل نافذة على حدة (30 نافذة)، لا `mean_ic` وحده: **كل نافذة من الثلاثين سالبة الإشارة على `high` بلا استثناء واحد** (من -0.018 إلى -0.39، والقيمة تشتدّ تدريجياً مع النوافذ الأحدث حين يتوفّر عدد أصول أكبر حيّة معاً فيقلّ ضجيج القياس)، **وكل نافذة موجبة الإشارة على `low` بلا استثناء واحد**. نمط متّسق مع أثر حقيقي متزايد الوضوح مع القوة الإحصائية، لا صدفة ناتجة عن نافذة أو أصل شاذّ واحد. الجدول الكامل (30×2 نافذة، منسوخ كما ورد بلا إعادة حساب) موجود في خلية التسجيل نفسها بالدفتر.

## ⚠️ تحذير منهجي: هذان ليسا اكتشافين مستقلّين

`NATR_neg_vol` هي ببساطة `-NATR_14` (تقلّب مُطبَّع)، و`RSI_vol_adjusted_reversion` هي `-(RSI_14-50)/(NATR_14+1e-6)` — أي RSI مقسوماً على نفس `NATR_14`. تقارب حجم الأثر شبه التام بينهما (-0.1970 مقابل -0.1980؛ +0.1644 مقابل +0.1667) يشير إلى أن **`NATR_14` نفسه هو المحرِّك الأساسي لكلا الإشارتين**، لا تأكيداً مستقلّاً من مؤشّرين مختلفين.

**الفرضية الفعلية الواحدة المسجَّلة**: تقلّب مرتفع حالياً (`NATR_14`) يسبق انكماشاً في نطاق الشمعة القادمة (`high` أدنى، `low` أعلى) — أي انكماش نطاق الحركة بعد فورة تقلّب (تفريغ التقلّب/استنفاد الزخم بعد حركة حادّة) — تفسير سلوكي معقول، لا نمط بلا معنى.

## أسئلة مفتوحة (قرار صريح من صاحب المشروع مطلوب قبل أي اعتماد عملي)

1. هل تصمد بعد تصحيح صريح للاختبارات المتعددة (26 مرشّحاً × 3 أهداف = 78 اختباراً في هذا التشغيل وحده — لم يُطبَّق تصحيح مثل Benjamini-Hochberg بعد)؟
2. هل تصمد أمام تحويل رتيب مختلف يفكّك أثر `NATR_14` عن التطبيع بـRSI تحديداً؟
3. الأهمّ عملياً: هل تنتقل فعلاً لتحسين حقيقي في تدريب `NIG-TimeNet v2` الكامل عند إضافتها كميزة، أم تبقى IC نظرياً محسوباً خطياً بلا أثر عملي على نموذج غير خطي مُدرَّب فعلياً؟

لا شيء من الثلاثة نُفِّذ بعد — هذه خطوات تالية صريحة، لا تنفيذ تلقائي.

**بداية للسؤال (٢)**: أُضيف مرشّح `Fractal_reversal_w5` إلى `signal_discovery_lab.ipynb` (القسم ١٢) — فركتالات ويليامز على `high`/`low` الخام، مستقلّة رياضياً عن `NATR_14`/`RSI` تماماً. إن أظهرت `consistent_sign=True` أيضاً، هذا دليل مستقلّ على أن "الارتداد بعد تطرّف محلي" أثر عام لا خاص بالتقلّب المُطبَّع تحديداً. لم يُشغَّل بعد — يحتاج إعادة بناء `dataset` بـ`exclude_from_features=["open", "volume"]` (إبقاء `high`/`low`، المُستبعَدتين افتراضياً في `crypto_data_pipeline_v6.ipynb`).
