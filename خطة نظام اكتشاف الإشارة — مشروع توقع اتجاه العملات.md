# خطة نظام اكتشاف الإشارة — مشروع توقع اتجاه العملات

Sep 23, 2026 · @Someone

## تحديث الحالة (24 سبتمبر 2026)

المراحل ٠-٣ منتهية عملياً. **ثلاث فرضيات مسجَّلة رسمياً حتى الآن (H001، H002، H003)**؛ H003 — انعكاس/انكماش التقلّب المدفوع بـ`NATR_14` — هي أول فرضية مقبولة رسمياً في تاريخ المشروع، اكتُشفت أثناء إعادة اختبار مكتبة المرشّحين القياسية على عيّنة موسّعة (50 أصلاً بدل 5، `market_context.enabled=True`). التفاصيل الكاملة (المنهجية، الأرقام، التحذير المنهجي، الأسئلة المفتوحة) في [`docs/research/h003_volatility_reversal.md`](docs/research/h003_volatility_reversal.md) — راجع [`docs/research/README.md`](docs/research/README.md) لفهرس كل الفرضيات والمراحل.

**بنية دفاتر `signal_evaluation_axis` تغيّرت**: كان محور التقييم وكل الفرضيات المُختبَرة عبره في ملف واحد كبير (`signal_evaluation_axis (3).ipynb`). أُفرِد الآن إلى دفتر أساسي (المحور فقط: IC، فحص عُشر، خطّ أساس عشوائي، سجلّ التجارب) + دفتر مستقلّ لكل فرضية (`hypothesis_h001_short_term_reversal.ipynb`، `hypothesis_h002_classification_head.ipynb`، `hypothesis_h003_volatility_reversal.ipynb`)، كلٌّ يعتمد على الأساسي عبر `%run` — بنفس أسلوب `main.ipynb` في تجميع الدفاتر. هذا يمنع تضخّم ملف واحد إلى ما لا نهاية مع كل فرضية جديدة، ويسمح بفتح فرضية واحدة بمعزل عن البقية.

**الخطوة القادمة**: الأسئلة المفتوحة الثلاثة في صفحة H003 — (أ) تصحيح الاختبارات المتعددة، (ب) هل الأثر خاص بـ`NATR_14`/`RSI` تحديداً أم أعمّ، (ج) هل يتحوّل لتحسين حقيقي في تدريب `NIG-TimeNet v2` الفعلي. مرشّحان أُضيفا لاختبار السؤال (ب) من زاوية مستقلّة عن `NATR_14`/`RSI`: فركتالات الانعكاس (Williams Fractals) — (١) مرشّح فحص سريع بلا تدريب في `signal_discovery_lab.ipynb` (القسم ١٢)، و(٢) ميزة سببية كاملة جاهزة في خط الأنابيب نفسه (`crypto_data_pipeline_v6.ipynb`، `custom_settings["fractal_windows"]`، افتراضياً `[]` أي مُعطَّلة) لو تحوّل الأمر لاختبار فعلي داخل `NIG-TimeNet v2`. راجع "تقطير الرؤية المتأخّرة" أدناه لسبب معالجة `center=True` بعناية (وكيف حُلّت بالإزاحة السببية لا بتصفير آخر خطوتين). لم يُفعَّلا بعد على أي dataset حقيقي — تفعيل الميزة الثانية لا يحتاج تعديل `exclude_from_features` إطلاقاً (تُحسَب من `high`/`low` الخام قبل استبعادهما)، فقط `fractal_windows=[5]`.

## لماذا مشروع منفصل — لا امتداد لدفتر التحضير

المشكلة التي دفعت لهذا القرار: كل إضافة سابقة (سياق سوقي، تطبيع مقطعي، تقسيم دوري) دخلت الدفتر الرئيسي قبل أن تُقاس قيمتها. هذا يُنتج تراكماً بلا دليل — بالضبط ما وُصف بـ"التضخّم بلا جدوى".

الفصل هنا حرفي: هذا المشروع لا يُنتج نموذجاً، بل يُنتج قراراً واحداً لكل فرضية: تُقبل (تدخل دفتر التحضير الرئيسي) أو تُرفض (تُوثَّق ولا تُلمَس مجدداً إلا بدليل جديد). لا حالة وسطى، ولا فرضية "تبدو واعدة" تدخل الإنتاج بلا رقم يثبتها.

القاعدة الحاكمة لكل قرار في هذه الخطة: أي فرضية أو أداة، مهما بدت ذكية، عديمة القيمة حتى تمرّ عبر محور تقييم واحد موحّد (تفصيله في قسم الأدوات ونماذج الاكتشاف). هذا يعني أن بناء محور التقييم نفسه يسبق أي فرضية — لا استثناء، حتى للفرضيات "الأساس" البديهية.

ما هذا المشروع ليس له:

ليس بديلاً عن نموذج التنبؤ الكبير (trainer_framework) — هو مورِّد فرضيات له فقط.

ليس مكاناً لهندسة الإنتاج (رام، تقسيم، استئناف) — تلك مسؤولية دفتر التحضير، ولن تتكرّر هنا.

ليس محاولة لإثبات أن إشارة موجودة — محاولة لمعرفة هل هي موجودة أصلاً، بدليل رقمي، حتى لو كانت الإجابة "لا".

## الأولويات

الترتيب هنا ليس حسب الإثارة، بل حسب رخص الدحض: كل بند يُختبَر بأقل جهد ممكن قبل الانتقال للأغلى، وكل بند لاحق يعتمد على أن السابق موجود وصحيح.

### ١) محور التقييم — أولوية قصوى، يسبق كل شيء

IC خارج العيّنة + فحص العُشر + خطّ أساس عشوائي (label shuffle)، عبر rolling_splits. بلا هذا، كل رقم لاحق ("IC=0.03"، "دقة 54%") بلا معنى — لا مرجع نقارنه به. لن يُختبَر أي شيء آخر قبل أن يعمل هذا المحور وحده على فرضية معروفة الإجابة (زخم بسيط مثلاً) للتأكد أنه يقيس ما يُفترض أن يقيسه.

### ٢) فرضيات الأساس (baseline) + ما هو مبنيّ أصلاً

funding rate وopen interest — البنية جاهزة من دفتر التحضير (v5)، غير مُفعَّلة في الميزات بعد. زخم/انعكاس قصير المدى كفرضيات مرجعية معروفة الاتجاه العام أكاديمياً — تُستخدَم لاختبار سلامة المحور نفسه قبل الثقة بنتائجه على أي شيء جديد.

### ٣) الفرضيات الاستكشافية

التفاعلات بين الميزات، السياق العابر للأصول، أنماط OI×السعر. تكلفة حسابية معتدلة، ومنطق سلوكي واضح لكل واحدة — أقرب لما اقترحه النموذج الآخر في ردّيه.

### ٤) أدوات الاكتشاف بلا قواعد مسبقة

Matrix Profile، الترميز التلقائي، العنقدة — الأغلى حسابياً والأصعب تفسيراً. تُفتح فقط بعد أن يثبت المحور نفسه سلامته على (٢) و(٣)، لأن نتائجها أصعب تصديقاً بالعين المجرّدة فتعتمد أكثر على سلامة القياس.

### ٥) الفرضيات الفريدة والمتطرفة

أرخص أولوية تنفيذ، لكن ليست الأقل قيمة بالضرورة — بعضها (كشف الشذوذ كإشارة، مثلاً) رخيص الاختبار فعلياً رغم غرابته. تُختبَر بالتوازي مع (٣) إن كانت تكلفتها الحسابية منخفضة، لا بالضرورة بالترتيب.

## منهجية الاكتشاف — أربعة مسارات متوازية

لا مسار واحد كافٍ بمفرده. كل مسار يغطي ثغرة لا يغطيها الآخر:

### أ) مبنيّ على الفرضية (Hypothesis-driven)

ابدأ بسؤال "لماذا يتحرّك السعر؟" قبل أي كود — تماماً كما وصف الذكاء الاصطناعي الآخر. أقلّ خطراً من ناحية مخاطر الاختبارات المتعددة (فرضية واحدة تُختبَر بنيّة واضحة، لا مئة ميزة تُغربَل بحثاً عن ناجحة صدفة)، لكن محدود بما يستطيع الخيال البشري تخيّله.

### ب) مبنيّ على البيانات (Data-driven / استكشافي بلا فرضية مسبقة)

عنقدة، تقليل أبعاد، Matrix Profile — يكتشف بنية لم تكن لتخطر ببال أحد. الخطر المقابل: أي نمط يظهر يحتاج تفسيراً بأثر رجعي ("لماذا هذا منطقي؟") قبل الثقة به، وإلا فهو مرشّح قويّ لأن يكون ضجيجاً بُولِف تفسيراً — هذا بالضبط فخّ "مخاطر الاختبارات المتعددة" الذي حذّر منه الردّ الثاني.

### ج) مبنيّ على الأدبيات (Literature-mining)

أنماط موثّقة أكاديمياً في أسواق أخرى (الأسهم، العملات التقليدية) — momentum، mean-reversion، عوامل السيولة — تُنقَل لسياق الكريبتو وتُختبَر من جديد بمحور التقييم نفسه، لا تُفتَرض صحيحة لأنها "نُشرت". أغلب عوامل الأسهم المعروفة (factor zoo) لا تنطبق حرفياً على الكريبتو (بنية سوق مختلفة، ساعات تداول متواصلة، رافعة أعلى)، لكنها نقطة بداية أرخص من الاختراع من الصفر.

### د) البحث الموجَّه (برمجة جينية / بحث عشوائي منظَّم)

يولّد تعبيرات رياضية من الميزات الخام آلياً، يُبقي الأفضل أداءً عبر أجيال متعاقبة (تفصيله في قسم الأدوات). الأخطر على الإطلاق من ناحية الاختبارات المتعددة — يجرّب آلاف التركيبات، فاحتمال نجاح إحداها صدفة شبه مؤكّد. لا يُستخدَم إلا مع تصحيح إحصائي صارم (تقسيم بيانات منفصل تماماً للتحقّق النهائي، لم يرَه البحث الجيني إطلاقاً) وبعد أن تثبت المسارات الثلاثة الأخرى أنها تستنفد أفكارها.

كيف تتكامل: الأربعة تصبّ في نفس محور التقييم (IC + عُشر + خطّ أساس عشوائي). المسار لا يُحدّد معيار القبول — يُحدّد فقط من أين أتت الفرضية؛ القبول واحد للجميع.

## طرق توليد فرضيات جديدة

المؤشرات (RSI، MACD، Bollinger...) استُهلكت — فُحصت آلاف المرات، بما في ذلك من قِبَل صاحب هذا المشروع نفسه. لكن آليات توليد الفرضية أقل استهلاكاً بكثير من الأدوات نفسها. خمس آليات، كل واحدة تفتح مساحة بحث مختلفة عن "جرّب مؤشراً جديداً":

### ١) مقلوب الافتراض الضمني (Assumption inversion)

كل مؤشر مبني على افتراض ثابت لم يُشكَّك فيه أحد. RSI يفترض أن حدّي 30/70 لهما معنى ثابت بصرف النظر عن حالة السوق — ماذا لو كانت العتبة نفسها متكيّفة مع نظام التقلّب (تتمدّد في سوق متقلّب، تنكمش في سوق هادئ)؟ ليس مؤشراً جديداً، بل كسر افتراض في مؤشر قديم — مساحة لم تُستهلك لأن أغلب من اختبر RSI لم يُشكّك في ثباته.

### ٢) تقطير الرؤية المتأخّرة (Hindsight distillation)

مؤشرات "مُعاد الرسم" (repainting) تُعامَل عادة كعيب يُتجنَّب. لكن إن كانت ممتازة بأثر رجعي (تحدّد مناطق دعم/مقاومة بدقة عالية بعد اكتمال البيانات)، يمكن إعادة توظيفها كهدف تنبّؤ لا كمدخل حيّ: يتعلّم النموذج توقّع ما ستستقرّ عليه القيمة المُعاد رسمها، من مدخلات حيّة فقط. تفصيلها في كتالوج الفرضيات (أضيفت للتوّ كمثال حيّ من تجربة سابقة لصاحب المشروع مع DPO/ATR).

### ٣) كسر التناظر عمداً (Asymmetry hunting)

أغلب المؤشرات تعامل الصعود والهبوط بنفس المعادلة. سلوكياً هذا غير واقعي — الذعر يتحرّك أسرع من الجشع، موثَّق نفسياً وسوقياً. مؤشر بمعادلتين مختلفتين للاتجاهين عمداً (لا نفس الصيغة بعلامة معكوسة) يلتقط ديناميكية حقيقية تتجاهلها أغلب المؤشرات القياسية بتصميمها.

### ٤) تحليل الأخطاء المنهجية للنموذج الحالي (Residual mining)

بدل تخمين فرضية من الصفر: افحص أين يُخطئ النموذج الحالي بانتظام، لا عشوائياً. أخطاء مُركَّزة في نطاق زمني أو نظام سوقي معيّن تشير مباشرة لمتغيّر غائب — مصدر فرضيات مُشتقّ من بيانات هذا المشروع تحديداً، لا من أدبيات عامة.

### ٥) النقل من مجال مختلف تماماً (Analogical transfer)

تقنيات كشف نقاط التغيّر في معالجة الإشارات، أو كشف الشذوذ في مراقبة الشبكات، لم تُصمَّم أصلاً كـ"مؤشرات تداول" — مساحة استكشافها بالكاد لُمِسَت في هذا السياق تحديداً.

## كتالوج الفرضيات

كل فرضية: المنطق السلوكي (لماذا قد تعمل)، التكلفة (بيانات/حساب)، والمخاطرة الرئيسية.

### أساس — تأكيد أن المحور والأدوات تعمل

| الفرضية | المنطق | التكلفة | المخاطرة |
| --- | --- | --- | --- |
| زخم قصير المدى (momentum) | استمرار الاتجاه بعد حركة قوية — موثّق أكاديمياً في أصول عالية التقلّب | منخفضة (عائد + نافذة متدحرجة) | معروف أنه يضعف/ينعكس أحياناً في الكريبتو تحديداً — نتيجته المتوقَّعة إشارة ضعيفة موجبة، لا معدومة؛ إن جاءت صفراً فالمشكلة في المحور لا في الفرضية |
| انعكاس قصير المدى (short-term reversal) | حركة حادة تفرط في التصحيح ثم ترتدّ جزئياً | منخفضة | يتداخل مع الزخم أحياناً حسب الأفق الزمني — يحتاج ضبط الأفق بعناية |
| funding rate كمقياس تموضع متطرف | funding مرتفع جداً = طويلون مفرطون بالرافعة → خطر تصفية → انعكاس هبوطي محتمل | مبنية أصلاً (fetch_funding_rate)، تحتاج فقط دمجاً ومحاذاة زمنية | تكرار جزئي مع الزخم نفسه إن لم يُفصَل أثرهما |

### استكشافية

| الفرضية | المنطق | التكلفة | المخاطرة |
| --- | --- | --- | --- |
| تفاعلات (حجم×جسم الشمعة، تباعد RSI) | حجم غير عادي مع جسم كبير = دخول لاعب كبير، لا ضجيج عادي | منخفضة — ضرب ميزتين موجودتين | عدد التوليفات يكبر بسرعة (فخّ الاختبارات المتعددة إن جُرِّبت كلها دفعة واحدة) |
| ربع OI×السعر (الجدول الذي شرحته سابقاً) | OI صاعد+سعر صاعد = اقتناع حقيقي؛ OI هابط+سعر صاعد = تغطية قصيرة هشّة | تحتاج دمج open_interest (مبني، غير مُفعَّل) | الفائدة المفتوحة محدودة تاريخياً بآخر ٣٠ يوماً من Binance — لا تصلح لاختبار تاريخي عميق إلا بعد تراكم أرشيف حقيقي عبر الزمن |
| سياق عابر للأصول (قوة نسبية لـBTC) | عملة تتفوّق على BTC في نفس الاتجاه = قوة قطاعية حقيقية لا انجراف سوقي عام | مبنية أصلاً (market_context) | ⚠️ **مُفعَّلة افتراضياً بلا محتوى** — راجع الحاشية أسفل الجدول: `config['market_context']['enabled']` يساوي `False` افتراضياً في `crypto_data_pipeline_v6`، فيصبح `MKT_ret_1` صفراً للجميع بصمت (لا خطأ)، لا "لا معلومة فوق العائد الخام" |
| تغيّر النظام (regime) كميزة سياقية | نفس الميزة قد تعني أشياء مختلفة في سوق هادئ مقابل متقلّب | معتدلة (HMM/GMM على التقلّب والاتجاه) | تصنيف نظام خاطئ يُدخل ضجيجاً بدل تقليله |

**⚠️ حاشية مُكتشَفة أثناء تنفيذ `signal_discovery_lab` (٢٤ سبتمبر ٢٠٢٦):** مرشّح `MKT_beta` (`المرحلة ٢`) سجّل `mean_ic=0.000000`، `std_ic=0.000000` **بالضبط** على الأهداف الثلاثة عبر كل النوافذ (12 ثم 56 نافذة) — تحقّق مباشر من قيم `MKT_ret_1` في الـdataset المُستخدَم أثبت أنه **صفر حرفياً لكل عيّنة ولكل خطوة زمنية**، لا مجرّد إشارة ضعيفة. السبب الجذري (تتبُّع الكود حتى `add_market_context`/`build_market_context` في `crypto_data_pipeline_v6.ipynb`): `build_market_context` تُرجع `None` فوراً إن كان `config['market_context']['enabled']` غير `True` (`mc.get('enabled', False)` — الافتراضي `False`)، وعندها تملأ `add_market_context` أعمدة `MKT_` بصفر **لكل الأصول بلا استثناء ولا تحذير يظهر للمستخدم النهائي** (التحذير يُطبَع فقط إن فشل تحميل BTC نفسه بعد تفعيل الميزة، لا حين تكون معطَّلة أصلاً). هذا **تناقض تصميمي** مع `FUND_rate_z` (تفشل بخطأ `ValueError` صريح حين تكون funding/OI معطَّلة — "فشل صريح لا صمت"، المبدأ الموثَّق في `signal_discovery_lab`) — `MKT_ret_1` تفشل *بصمت* بدلاً من ذلك لأنها تبقى موجودة في `feature_order` بقيمة صفرية، لا غائبة تماماً. **الأثر العملي:** كل نتيجة `MKT_beta`/`market_beta` في هذا المشروع (H001، H002، `signal_discovery_lab`) حتى تاريخه **لم تختبر الفرضية فعلياً إطلاقاً** — وليست "فرضية مرفوضة بدليل"، بل "فرضية لم تُختبَر بعد". **الإصلاح المطلوب قبل أي حكم على هذه الفرضية**: تفعيل `config['market_context']['enabled']=True` صراحةً عند بناء أي dataset مستقبلي أريد اختبار `MKT_beta` عليه، والتحقّق بعدها أن `MKT_ret_1` غير صفري فعلاً قبل الاعتماد على أي IC يُحسَب منه.

### فريدة ومتطرفة — رخيصة التجربة رغم غرابتها

| الفرضية | المنطق | التكلفة | المخاطرة |
| --- | --- | --- | --- |
| الشذوذ نفسه كإشارة (لا كضجيج يُستبعَد) | حركة شاذة إحصائياً قد تعكس حدثاً حقيقياً (تصفية جماعية، خبراً) لا ضجيجاً عشوائياً | منخفضة (Isolation Forest جاهزة في scikit-learn) | تفسير الشذوذ بعد وقوعه سهل، توقّعه قبله هو الاختبار الحقيقي |
| زخارف زمنية متكرّرة (motifs) عبر Matrix Profile | أنماط تتكرّر شكلياً بلا افتراض مسبق عن هيئتها | معتدلة-عالية | يكتشف أن نمطاً تكرّر، لا لماذا — يحتاج تفسيراً بأثر رجعي قبل الثقة |
| تمثيلات مُتعلَّمة (autoencoder) بدل مؤشرات يدوية | ربما توجد بنية في شكل النافذة لا تلتقطها أي صيغة مؤشر معروفة | عالية (تدريب شبكة منفصلة) | صندوق أسود — صعب تبرير أي قرار تداول مبنيّ عليه لاحقاً |
| ترابط متغيّر بين العملات (تحليل شبكي) | قوة/ضعف ترابط عملة بأخرى يتغيّر قبل تحرّكات كبيرة أحياناً | عالية | تعقيد تنفيذي كبير مقابل دليل أوّلي غير مؤكَّد على قيمته في هذا السياق تحديداً |

### تقطير الرؤية المتأخّرة — مثال حيّ: DPO/DBO × ATR

من تجربة سابقة: تقاطع DPO(14)/ATR بتأخير 14 شمعة (النسخة المُعاد رسمها بالكامل) التقط أنماطاً ممتازة عند تحديد مناطق دعم/مقاومة. بتأخير شمعة واحدة فقط، النتائج بقيت ممتازة. بلا تأخير (الاستخدام الحيّ المباشر)، النتائج تباينت — لا خسائر كبيرة، لكن لا ربح واضح أيضاً.

لماذا هذا متوقَّع رياضياً، لا خللاً في التجربة: الصيغة الكلاسيكية لـDPO تُزيح المتوسط المتحرّك للخلف، أي قيمتها عند اللحظة t تعتمد فعلياً على شموع مستقبلية نسبة لتلك اللحظة. الاستخدام الحيّ (بلا تأخير) يستخدم نسخة غير مكتملة حسابياً؛ من الطبيعي أن تُعطي نتيجة بينية لا كارثية.

إعادة الصياغة المقترحة: لا يُستخدَم DPO المُعاد رسمه كمدخل (تسرّب معلومات مستقبلية، غير قابل للتنفيذ حيّاً) — يُستخدَم كهدف. يبني النموذج تصنيفاً ("هل سيحدث تقاطع صاعد/هابط خلال N شمعة قادمة") من مدخلات حيّة فقط (DPO وATR بصيغتهما القياسية غير المُزاحة)، بنفس آلية forecast_horizon الموجودة أصلاً في دفتر التحضير — فقط بنافذة أطول (14 شمعة) وفجوة عزل مطابقة لها.

| الجانب | التفصيل |
| --- | --- |
| المنطق | مؤشر ممتاز بأثر رجعي عند تحديد نقاط الانعكاس، عديم الفائدة حيّاً بصيغته الأصلية — الفكرة تُقطِّر تلك الرؤية المتأخّرة في نموذج تنبّئي |
| التكلفة | منخفضة — لا بنية جديدة، توسعة لآلية الهدف الموجودة بأفق أطول |
| المخاطرة الرئيسية | لا يتحايل على كفاءة السوق: DPO(t+14) دالة حتمية في أسعار المستقبل نفسها، فصعوبة توقّعه مماثلة لصعوبة توقّع شكل الحركة القادمة. الميزة هندسية فقط — هدف "هل ستتشكّل منطقة دعم/مقاومة هنا" قد يكون نسبة إشارة-إلى-ضجيج أنظف من العائد الخام، لا أسهل توقّعاً بالضرورة |

تعميم: أي مؤشر "ممتاز بأثر رجعي، عديم الفائدة حيّاً" (Zigzag، نقاط الانعكاس الكسورية، القمم/القيعان المؤكَّدة) يصلح لنفس النمط — عائلة فرضيات كاملة، لا فرضية واحدة.

**⚠️ مثال حيّ لنفس الفخّ (24 سبتمبر 2026):** فركتالات ويليامز (`high.rolling(w, center=True).max()`) هي بالضبط "نقاط الانعكاس الكسورية" المذكورة أعلاه — `center=True` تجعلها مُعاد رسمها فعلياً (تحتاج `window//2` شمعة "مستقبلية" لتأكيد أي نقطة). خلافاً لـDPO/ATR (يُقطَّران كهدف تنبّؤ جديد)، الحلّ هنا أبسط: **إزاحة** (`shift`) الناتج الخام بمقدار `window//2` شمعة (لا تصفير آخر خطوتين يدوياً — الإزاحة العادية غير الدائرية تكفي وحدها، إذ تضع القيم غير المعروفة في *بداية* السجلّ كاملاً، لا في نهاية كل نافذة؛ راجع اختبار السببية `_test_fractal_features_causal` للتحقّق العددي) يكفي لجعلها مدخلاً حيّاً بلا أي تسرّب، عند كل خطوة زمنية بما فيها آخر خطوة في أي نافذة — لا تحويلها إلى هدف منفصل، ولا حاجة لأي إزاحة دائرية (`np.roll`، ستُدخل بيانات من نهاية السجلّ إلى بدايته خطأً). مُنفَّذة بهذا التصحيح في مكانين: (١) `signal_discovery_lab.ipynb` (القسم ١٢، `Fractal_reversal_w5`) كمرشّح فحص سريع بلا تدريب، و(٢) `crypto_data_pipeline_v6.ipynb` (`_fractal_columns`/`custom_settings["fractal_windows"]`) كميزة سببية كاملة لكل خطوة زمنية جاهزة لخط الأنابيب — راجع تحديث الحالة أعلى هذا الملف.

## أدوات ونماذج لاكتشاف نمط غير معروف

هذا القسم يجيب تحديداً على "نمط لا يمكن وضع قواعد له لأنه غير معلوم" — أدوات لا تفترض شكل الإشارة مسبقاً، بل تكتشف البنية من البيانات نفسها. كل أداة هنا تُنتج مرشّحاً، لا إشارة مؤكَّدة — كلها تمرّ عبر محور التقييم قبل أي قرار.

### Matrix Profile (اكتشاف الزخارف الزمنية) — الأنسب لسؤالك تحديداً

يمسح السلسلة الزمنية ويجد كل نافذة تتشابه شكلياً مع نافذة أخرى في مكان مختلف من التاريخ ("زخارف" motifs)، بلا أي افتراض مسبق عن شكل الزخرفة. مكتبة stumpy (بايثون) تنفّذه بكفاءة. التكلفة: معتدلة (O(n log n) تقريباً بخوارزميات حديثة، لا O(n²) الساذجة). الاستخدام هنا: إيجاد أنماط سعرية متكرّرة، ثم فحص: هل العائد بعد كل تكرار للزخرفة نفسها متسق الاتجاه؟ هذا رقم يُغذّى مباشرة لمحور IC.

### العنقدة غير المُشرَفة (K-means / GMM / HDBSCAN)

تُجمِّع نوافذ الأسعار (بعد تطبيعها) في مجموعات متشابهة الشكل. الاستخدام: هل عضوية عنقود معيّن ترتبط بعائد مستقبلي مختلف عن البقية؟ HDBSCAN تحديداً لا تفترض عدد العناقيد مسبقاً ولا تُجبر كل نقطة على الانضمام لعنقود (تترك الضجيج بلا تصنيف) — أنسب من K-means لبيانات مالية نادراً ما تُشكِّل كتلاً كروية نظيفة.

### الترميز التلقائي (Autoencoder)

شبكة تتعلّم ضغط النافذة إلى تمثيل أصغر (الفضاء الكامن) ثم إعادة بنائها، بلا إشراف. الاستخدام: فحص إن كانت أبعاد الفضاء الكامن (لا الميزات اليدوية) ترتبط بالعائد المستقبلي. التكلفة: الأعلى في هذه القائمة (تدريب شبكة منفصلة، ثم تفسير أبعادها). المخاطرة: صندوق أسود — إشارة تُكتشَف هنا صعبة التبرير لاحقاً أمام أي قرار تداول مبنيّ عليها.

### غابة عشوائية ضحلة + SHAP — لا للتنبؤ، بل للاستكشاف

درّب غابة عشوائية بعمق محدود (3-4) على كل ميزاتك الخام دفعة واحدة، ثم افحص SHAP interaction values — تكشف أي أزواج ميزات تتفاعل فعلياً في قرارات الشجرة، آلياً، بلا تحديد التفاعلات يدوياً مسبقاً (كما فعل الردّ الثاني بضرب حجم×جسم الشمعة يدوياً — هذا يكتشف تفاعلات مشابهة أوتوماتيكياً، وربما تفاعلات لم تكن لتخطر ببالك). التكلفة: منخفضة نسبياً، ومباشرة (scikit-learn + shap).

### كشف الشذوذ (Isolation Forest / One-Class SVM)

يعزل النقاط الشاذة إحصائياً دون تعريف مسبق لما يجعلها شاذة. الاستخدام: فرضية أن الشذوذ نفسه (لا محيطه) يحمل معلومة — عكس الاستخدام المعتاد (تنظيف الشواذ)، هنا الشاذ هو الهدف.

### البرمجة الجينية (gplearn أو مكافئ)

تولّد تعبيرات رياضية من الميزات الخام ((RSI - 50) / (volume_z + 1) مثلاً) عشوائياً، تُبقي الأفضل أداءً عبر أجيال متعاقبة، وتُكرِّر. الأقوى في اكتشاف تفاعلات غير بديهية، والأخطر في الاختبارات المتعددة — آلاف التعبيرات تُجرَّب، فبعضها سينجح صدفة حتماً. إلزامي معها: طيّة تحقّق نهائية معزولة تماماً لم تَرَها عملية التطوّر إطلاقاً، لا فقط test العادي (الذي قد يكون "رآه" الباحث بشرياً عبر التكرار).

### كيف تُقيَّم نتائج كل هذه الأدوات

لا فرق في المعاملة بين نمط اكتشفته شجرة قرار ونمط اقترحه حدسك: الجميع عبر نفس محور IC + عُشر + خطّ أساس عشوائي. الأدوات هنا تُغيّر مصدر المرشّح لا معيار قبوله — هذا هو الضمان الوحيد ضد الوقوع في فخّ "وجدت نمطاً!" الذي هو في الحقيقة ضجيج مُلبَّس تفسيراً بأثر رجعي.

## خطة بناء النظام — تسريع التجارب

الهدف: اختبار فرضية جديدة يجب أن يكون دقائق لا ساعات. أربع ركائز:

### ١) واجهة موحّدة لكل فرضية

كل فرضية (مهما كان مصدرها — يدوية، Matrix Profile، برمجة جينية) تنتهي بنفس الشكل: دالة واحدة تُدخِل عمود ميزة (أو أكثر) إلى إطار بيانات موجود. لا فرضية تُبنى بأسلوبها الخاص المنفصل — هذا وحده يمنع إعادة كتابة منطق المحاذاة الزمنية والتغذية لكل فرضية جديدة.

### ٢) ذاكرة تخزين مؤقت للميزات المحسوبة

OHLCV الخام نفسه لا يتغيّر بين تجربة وأخرى — إعادة تحميله وإعادة حساب المؤشرات الأساسية لكل فرضية جديدة هدر صرف. طبقة تخزين مؤقت (على القرص، بمفتاح = بصمة الإعدادات، بنفس روح _checkpoint_fingerprint المبنية أصلاً في دفتر التحضير) تحفظ الأساس المشترك مرة واحدة، وكل فرضية تضيف عمودها فوقه فقط.

### ٣) سجلّ تجارب (Experiment Registry)

كل فرضية = سجلّ واحد: معرّف، الفرضية بصياغة سطر واحد، المصدر (أيّ من المسارات الأربعة)، الحالة (قيد الاختبار/مقبولة/مرفوضة)، ونتائج المحور (IC عبر كل نافذة من rolling_splits، فحص العُشر، نتيجة خطّ الأساس العشوائي). الفائدة الحقيقية: يمنع اختبار نفس الفرضية (أو صياغة مقنَّعة منها) مرتين بعد نسيان أنها فشلت، ويكشف التكرار بين فرضيات "جديدة" وأخرى مقبولة أصلاً.

### ٤) تشغيل دفعي (Batch Runner)

يأخذ قائمة فرضيات (حتى لو عشرات) ويُشغّلها جميعاً عبر المحور نفسه بالتوازي (تُعاد استعارة أدوات التوازي والذاكرة المضبوطة من دفتر التحضير — نفس default_workers الواعي بالرام)، ويكتب النتائج مباشرة في سجلّ التجارب. هذا ما يجعل اختبار دفعة فرضيات استكشافية في جلسة واحدة ممكناً عملياً، لا نظرياً فقط.

ملاحظة تصميم مهمّة: هذه البنية بأكملها تُعاد استخدام مكوّنات دفتر التحضير الحالي (المحاذاة الزمنية، rolling_splits، إدارة الرام) لا تُعاد كتابتها من الصفر — المشروع البحثي طبقة تجريب فوق أدوات موجودة، لا نظام مستقلّ بالكامل.

## الترتيب الزمني — مراحل ببوّابة قبول/رفض بين كل اثنتين

كل مرحلة تحتاج نتيجة المرحلة السابقة لتبدأ — لا تنفيذ متوازٍ للمراحل نفسها (خلافاً للفرضيات داخل المرحلة الواحدة، التي تُختبَر دفعة واحدة عبر المُشغّل الدفعي).

### المرحلة ٠ — محور التقييم

IC + عُشر + خطّ أساس عشوائي، عبر rolling_splits. بوّابة الخروج: يُختبَر على فرضية الزخم البسيط (معروفة الاتجاه العام أكاديمياً) — إن أعطى نتيجة معقولة ومتّسقة عبر النوافذ، المحور موثوق للانتقال للمرحلة التالية. إن لم يُعطِ، المشكلة في القياس نفسه ولا معنى للمتابعة.

### المرحلة ١ — فرضيات الأساس + تفعيل funding/OI

تُختبَر عبر المحور نفسه. بوّابة الخروج: توثيق كل نتيجة (مقبولة أو مرفوضة) في سجلّ التجارب — لا حاجة لنتيجة إيجابية للانتقال، فقط لدليل موثَّق.

### المرحلة ٢ — الفرضيات الاستكشافية

التفاعلات، السياق العابر للأصول، أرباع OI×السعر. بوّابة الخروج: إن لم تُظهر أي فرضية IC ثابت عبر النوافذ، هذا مؤشّر مهم (لا نهائي) على أن ميزات إضافية من نفس العائلة (OHLCV+مشتقّاتها المباشرة) لن تضيف كثيراً — يرفع أولوية المرحلة ٣ بدل الاستمرار بمزيد من نفس النوع.

### المرحلة ٣ — أدوات الاكتشاف بلا قواعد

Matrix Profile، SHAP interactions، العنقدة. الأغلى حسابياً، فتُفتح فقط بعد استنفاد المرحلة ٢. بوّابة الخروج: أي مرشّح من هنا يحتاج تفسيراً سلوكياً بأثر رجعي قبل قبوله (لا يكفي IC جيد وحده — نمط بلا تفسير مرشّح أقوى لأن يكون ضجيجاً مُلبَّساً).

### المرحلة ٤ — الفرضيات المتطرفة والبرمجة الجينية

تُفتح فقط إن أعطت المراحل ١-٣ نتائج مشجّعة تستحق استثماراً أعلى — لا كخطوة تلقائية تالية. البرمجة الجينية تحديداً تحتاج طيّة تحقّق معزولة لم تُبنَ بعد.

القاعدة عبر كل المراحل: الانتقال من مرحلة لأخرى قرار صريح موثَّق، لا انزلاق تلقائي — تماماً الانضباط الذي كان غائباً في إضافة الميزات للدفتر الرئيسي بلا قياس.
