# مراجعة بنية النموذج — NIG-TimeNet v2

مراجعة مطلوبة صراحةً من صاحب المشروع: تدقيق كل مكوّن في `model_v2 (1).ipynb`
(لماذا وُجد، هل يؤدي وظيفته صحيحاً، هل هناك بديل أفضل، ما تكلفته مقابل
فائدته)، واختبار جدوى إضافة طبقة بيزية، مع تحديد هل أي تضخيم للنموذج مبرَّر
بنتائج أدقّ وما أثره على التعميم.

## طريقة المراجعة وحدودها

- قراءة مباشرة لكود كل مكوّن (لا افتراضات) من `model_v2 (1).ipynb`.
- بحث مُوجَّه في الإنترنت (سبتمبر ٢٠٢٦) للتحقّق من أحدث النقد/البدائل
  المنشورة لكل تقنية مستخدَمة — المصادر مذكورة تحت كل قسم.
- **لم يُجرَ اختبار تدريب فعلي مقارن** (بمعمارية بيزية مقابل الحالية) — هذا
  يحتاج تدريباً حقيقياً على بيانات حقيقية عبر نوافذ متعدّدة (تكلفة حوسبة
  عالية بلا GPU في بيئة التطوير الحالية)، وهو مؤجَّل عمداً حتى تُحسَم
  الأولوية الأهم أولاً: هل توجد أصلاً إشارة قابلة للتعلّم في الميزات الحالية
  (راجع `signal_discovery_lab.ipynb` — ~230 تجربة مرفوضة حتى تاريخه). هذه
  المراجعة إذن **تحليل كودي ومعماري + دعم من الأدبيات المنشورة**، لا نتيجة
  تجريبية على بيانات هذا المشروع تحديداً.

## جولة على كل مكوّن

### ١) `InstanceNorm` (تطبيع RevIN-style)

**الغرض**: تطبيع كل نافذة/ميزة لمتوسط وانحراف صفر بمعامل تعلُّم (affine)،
مع إرجاع `stats` (المتوسط ولوغاريتم الانحراف) لتُستخدَم لاحقاً في القراءة
النهائية — لا تُفقَد معلومة مستوى/تقلّب النافذة رغم التطبيع.

**هل تعمل صحيحاً؟** نعم — تصميم مطابق لـRevIN الأصلي (Kim et al., 2021)
تقريباً، لكن **بلا "denormalization" في المخرج** (النصف الثاني من RevIN
الأصلي، حيث يُعاد ضرب/جمع مخرج النموذج بنفس إحصاءات الإدخال). هذا **قرار
مقصود وموثَّق في الكود نفسه**: الأهداف هنا عوائد نسبية بلا وحدة قياس أصلاً
(`reg_target_mode='return'`)، فلا حاجة لعكس تطبيع لم يُطبَّق على الهدف.
قرار سليم، لا خطأ.

**البديل**: `LayerNorm` عادية (بلا إحصاءات لكل نافذة) أضعف لسلاسل مالية
متغيّرة التقلّب بشدة — RevIN تحديداً صُمِّمت لمعالجة "concept drift" الناتج
عن تغيّر التوزيع بين النوافذ، وهي مشكلة حقيقية في بيانات العملات (تقلّب
متغيّر جداً بين الأنظمة السعرية). **لا داعٍ لتغييرها.**

### ٢) `CausalMultiScaleDecomp` (تفكيك اتجاه/موسمية سببي متعدّد المقاييس)

**الغرض**: فصل `trend` (مزيج مُتعلَّم من متوسطات متحرّكة بأحجام 3/5/9/17)
عن `seasonal` (الباقي)، بحشو سببي (لا تسرّب من المستقبل).

**هل تعمل صحيحاً؟** نعم، وهي **أعمّ من Autoformer الأصلي** (Wu et al.,
2021) الذي يستخدم نافذة تفكيك واحدة ثابتة — هنا بوّابة (`gate`) مُتعلَّمة
تمزج 4 مقاييس حسب حالة كل نافذة (باستخدام `rough` = متوسط التغيّر المطلق
كإشارة تقلّب توجّه البوّابة). تصميم أكثر مرونة من الأصل المنشور، لا مجرّد
نسخ.

**البديل/التكلفة**: تكلفتها 4 عمليات `AveragePooling1D` + `Dense` صغيرة —
رخيصة جداً مقارنة بطبقات المحوّل. **مبرَّرة.**

### ٣) `PatchEmbedding` (تضمين رُقَع + مواضع مطلقة)

**الغرض**: تحويل النافذة (32 خطوة) إلى رموز (patches) بدل معاملة كل خطوة
زمنية كرمز مستقل — أقلّ تكلفة حسابية للانتباه، ويلتقط أنماطاً محلية.

**هل تعمل صحيحاً؟** نعم. نقطتان تستحقّان التنويه من البحث:
- تُستخدَم `Conv1D` للإسقاط (لا `Dense`/`Flatten` كما في PatchTST الأصلي
  Nie et al. 2023) — بحث ٢٠٢٤ يذكر أن تضمين الرُقَع القائم على CNN يتفوّق
  على استراتيجية الطبقة الخطّية لـPatchTST. **هذا الدفتر يستخدم فعلاً
  النسخة الأفضل موثَّقاً، لا الأصلية.**
- `patch_len=4, stride=2` على `seq_len=32` (≈15 رمزاً) — PatchTST الأصلي
  استُخدم على سلاسل أطول بكثير (336-720 خطوة، رُقَع 16 بطول 64 رمزاً).
  الأدبيات تذكر أن حجم الرقعة الأمثل يكبر مع طول المدخل — أي العكس صحيح
  لمدخل قصير كهذا: رقعة صغيرة (4) هي الخيار المتوقَّع نظرياً، لا نسخ أعمى
  لإعداد PatchTST الأصلي. **مُعايَر بشكل معقول لحجم المدخل الفعلي.**

المصادر: [PatchTST (arXiv 2211.14730)](https://arxiv.org/abs/2211.14730)،
[مقارنة تضمين الرُقَع 2024](https://dl.acm.org/doi/10.1145/3711507.3711508).

### ٤) `RMSNorm` + `RelativeGQAttention` + `SwiGLU` + `TransformerBlock` (كتلة محوّل بأسلوب Llama)

**الغرض**: كتلة محوّل حديثة (RMSNorm بدل LayerNorm، GQA بدل MHA الكامل،
SwiGLU بدل FFN بتنشيط ReLU/GELU عادي، انحياز موضع نسبي بأسلوب T5) — نفس
مكوّنات معماريات Llama تقريباً.

**هل تعمل صحيحاً؟** الكود سليم (تحقّق: `causal`/`window` masking، انحياز
نسبي مُقصوص بحدّ `max_rel_pos`، تهيئة أوزان مُصغَّرة بعمق الطبقة
`out_std = 0.02/sqrt(2*num_layers)` — ممارسة قياسية لثبات التدريب العميق).

**⚠️ ملاحظة معمارية مهمة — GQA هنا لا تُقدِّم فائدتها المعتادة:**
الغرض الأساسي من Grouped-Query Attention (Ainslie et al., تقنية Llama-2)
هو **تقليل حجم ذاكرة KV-cache أثناء التوليد التسلسلي (autoregressive
decoding)** — حيث يُعاد استخدام K/V المحسوبة لكل خطوة توليد جديدة، فتقليل
عدد رؤوس K/V يوفّر ذاكرة فعلية متراكمة عبر خطوات كثيرة. **هذا النموذج لا
يُولِّد تسلسلياً إطلاقاً** — تمريرة أمامية واحدة (single-shot) لكل نافذة
كاملة، بلا أي KV-cache يتراكم. أي: **فائدة GQA الأصلية (توفير ذاكرة
inference التراكمية) غير موجودة هنا أصلاً**؛ ما تبقّى هو فقط تقليل عدد
معاملات `wk`/`wv` (2 رأس بدل 4) — توفير معامِلات ضئيل على نموذج أصلاً صغير
(`d_model=128`)، مقابل تقليل تنوّع تمثيل K/V المحتمل. **ليست خطأً — النموذج
يعمل صحيحاً بها — لكنها تعقيد مستورَد من أدبيات LLM بلا الفائدة التي
صُمِّمت من أجلها في هذا السياق تحديداً.**

**التوصية**: خيار غير عاجل التغيير (لا يضرّ الصحة)، لكن لو أُعيد النظر في
البنية مستقبلاً: تبسيطها إلى انتباه عادي كامل الرؤوس (`num_kv_heads=
num_heads`) يُزيل هذا التعقيد بلا كلفة تُذكَر على نموذج بهذا الحجم، أو
الإبقاء عليها فقط إن كانت هناك خطة مستقبلية لتنبؤ تسلسلي متعدّد الخطوات
(عندها تصبح الفائدة الأصلية حقيقية).

المصدر: تقنية GQA الأصلية —
[GQA: Training Generalized Multi-Query Transformer Models (Ainslie et al.)](https://arxiv.org/abs/2305.13245).

### ٥) القراءة النهائية (`LastToken` + `AttentionPool` + `stats_proj` + المسار الخطّي المتبقّي)

**الغرض**: تجميع مخرجات المحوّل بثلاث طرق مختلفة معاً (آخر رمز، تجميع
بانتباه عبر كل الرموز، إحصاءات RevIN المُسقَطة) + **مسار خطّي مستقلّ تماماً
عن المحوّل** (`Flatten` للنافذة الخام المُطبَّعة → `Dense` بتنظيم L2 → جمع
مباشر مع مخرج المحوّل).

**هذا هو أهمّ قرار تصميمي في النموذج بأكمله من ناحية الأدبيات**: بحث Zeng
et al. 2022 ("Are Transformers Effective for Time Series Forecasting?")
أظهر أن نماذج خطّية بسيطة جداً (DLinear/NLinear) تتفوّق أحياناً على محوّلات
معقّدة في تنبؤ السلاسل الزمنية — خصوصاً على بيانات صغيرة أو قصيرة المدى.
**هذا الدفتر لا يتجاهل هذه النتيجة، بل يحصّن ضدّها مباشرة**: المسار الخطّي
موجود كجزء أصيل من المعمارية (لا بديل منفصل يُختبَر لاحقاً)، فحتى لو فشل
المحوّل في تعلّم شيء مفيد، يبقى للنموذج مسار خطّي بسيط يستطيع التقاط ما
يستطيعه DLinear على الأقل. **قرار تصميمي ممتاز وموثَّق ضمنياً — لا يحتاج
تعديلاً.**

الشيء الوحيد المفقود: لا توجد آلية لقياس **مساهمة كل مسار على حدة** (مثلاً:
معيار على وزن الجمع النسبي `h`/`lin`، أو تسجيل انحراف كل مسار بمعزل عن
الآخر) — لو أُريد التأكّد لاحقاً من أن المحوّل يضيف شيئاً فعلاً فوق المسار
الخطّي (لا العكس)، تسجيل هذا أثناء التدريب رخيص ومفيد تشخيصياً.

المصدر: [Are Transformers Effective for Time Series Forecasting? (Zeng et al., arXiv 2205.13504)](https://arxiv.org/abs/2205.13504).

### ٦) `NIGHead` (الانحدار الدليلي العميق — Deep Evidential Regression)

**الغرض**: إخراج توزيع Normal-Inverse-Gamma كامل (`mu, nu, alpha, beta`)
بتمريرة أمامية واحدة، يُشتقّ منه تحليلياً كل من التنبؤ ونوعا عدم اليقين
(aleatoric/epistemic) بلا حاجة لـMC-Dropout أو مجموعة نماذج (ensemble) —
هذا سبب تعطيل `use_uncertainty_head` (مسار MC-Dropout القديم) في إعدادات
خط الأنابيب فعلاً (موثَّق هناك: "عدم اليقين يأتي تحليلياً من نموذج NIG").

**هل تعمل صحيحاً؟** الصيغة الرياضية مطابقة لصيغة Amini et al. 2020 الأصلية
(`aleatoric = sqrt(beta/(alpha-1))`, `epistemic = sqrt(beta/(nu*(alpha-1)))`),
مع حدود دنيا (`nu_min/alpha_min/beta_min`) تمنع الانهيار العددي — ممارسة
قياسية موثَّقة في التطبيقات العملية للطريقة.

**⚠️ لكن الأدبيات الحديثة (٢٠٢٤-٢٠٢٥) توثِّق مشاكل حقيقية في هذه الطريقة
بالذات، لا انطباعاً عاماً:**
- **عدم تحديد فريد (non-identifiability)**: التفكيك بين "epistemic" و
  "aleatoric" غير فريد رياضياً خصوصاً في أنظمة الضجيج المنخفض — جزء من
  إشارة عدم يقين النموذج قد "يُمتَصّ" داخل قناة aleatoric بدلاً من epistemic.
- **مشكلة "انكماش الدليل" (evidence contraction)**: قيود عدم السلبية على
  معاملات NIG (عبر `softplus`) تُسبِّب انكماشاً في "الدليل" يُعيق التحسين —
  موثَّقة في AAAI 2024 كمشكلة محدَّدة بحلّ مُقترَح منفصل.
- **دلالة استدلالية لا بايزية حقيقية**: الكميات المُتعلَّمة "epistemic"/
  "aleatoric" لا تطابق دائماً الدلالة البايزية الصارمة — تتصرّف كدوال وكيل
  تتحكّم فيها ديناميكيات التحسين وقوّة المُنظِّم (regularizer)، لا الاحتمال
  الصارم.

**لكن — نقطة حاسمة لصالح إبقاء NIG هنا تحديداً**: أبحاث ٢٠٢٤ تذكر أن
**الانحدار الدليلي (evidential regression) يتفوّق على مجموعات النماذج (deep
ensembles) على البيانات الصغيرة**، بينما تتفوّق المجموعات فقط على بيانات
كبيرة وناعمة. بيانات هذا المشروع (365-700 عيّنة تدريب لكل نافذة) **صغيرة
بالضبط بهذا المعنى** — فاختيار NIG بدل مجموعة نماذج (التي تحتاج N تدريبات
كاملة لكل نافذة، تكلفة N أضعاف) مبرَّر تجريبياً بالأدبيات، لا مجرّد اختيار
هندسي عشوائي.

المصادر:
[Deep Evidential Regression الأصلية (Amini et al.)](https://arxiv.org/pdf/1910.02600)،
[نقد overparameterization/non-identifiability](https://www.researchgate.net/publication/371915365_The_Unreasonable_Effectiveness_of_Deep_Evidential_Regression)،
[مشكلة Evidence Contraction (AAAI 2024)](https://mlanthology.org/aaai/2024/wu2024aaai-evidence/)،
[تفوّق الطرق الدليلية على البيانات الصغيرة](https://arxiv.org/pdf/2607.11091).

### ٧) `OrderedMeans` (فرض `high ≥ close ≥ low`)

**الغرض**: قيد فيزيائي بسيط (شمعة OHLC صالحة دائماً `high≥close≥low`) عبر
`mu_close ± softplus(...)` بدل ثلاثة تنبؤات مستقلّة قد تتناقض فيزيائياً.

**تقييم**: قرار سليم ورخيص (طبقة بلا معاملات تقريباً) يمنع فئة كاملة من
الأخطاء المنطقية بلا أي تكلفة تدريب إضافية — **من أفضل القرارات الصغيرة في
هذه المعمارية.** لا بديل أفضل مطلوب.

### ٨) `ConfidenceHead` (ثقة وصفية، `stop_gradient` بالكامل)

**الغرض**: درجة ثقة توضيحية (٠٫٠٥-٠٫٩٨) من معاملات NIG + التمثيل الخفي،
لكن **مقطوعة التدرّج بالكامل** — لا تؤثّر إطلاقاً على تعلّم جذع التنبؤ،
تُستخدَم فقط لاحقاً في قرارات الامتناع (`abstention`) في خط الأنابيب.

**تقييم**: تصميم آمن ومقصود (التعليق في الكود يوضّح هذا صراحة) — فصل تام
بين "ما يُحسِّن التنبؤ" و"ما يُفسِّر الثقة به" يمنع أي حافز غير مباشر
للنموذج على "التلاعب" بالثقة المُبلَّغة لتقليل الخسارة. **لا حاجة لتغيير.**

## السؤال المحوري: هل إضافة طبقة بيزية مبرَّرة؟

**الإجابة المباشرة: لا، ليس الآن — وليس بسبب تكلفة الحوسبة وحدها، بل لأن
الأدبيات والسياق الفعلي لهذا المشروع كلاهما يشيران لاتجاه معاكس:**

1. **النموذج يملك أصلاً آلية عدم يقين تحليلية رخيصة (NIG)** — أي "طبقة
   بيزية" إضافية (MC-Dropout حقيقي، مجموعة نماذج، أو حتى Bayesian Last
   Layer/VBLL الأخفّ) ستكون **إضافة فوق آلية موجودة بالفعل**، لا سدّاً
   لفراغ. السؤال الصحيح ليس "هل نضيف بايزي؟" بل "هل نستبدل NIG بشيء أفضل
   أم نُبقيه؟"
2. **الأدبيات (٢٠٢٤) تُفضِّل الطرق الدليلية تحديداً على البيانات الصغيرة**
   — وهذا المشروع يعمل ببيانات صغيرة جداً لكل نافذة (365-700 عيّنة). مجموعة
   نماذج (deep ensembles، الطريقة البايزية الأكثر موثوقية عملياً) تحتاج
   حرفياً **N أضعاف تكلفة التدريب** (كل نافذة من الـ56 نافذة تحتاج N نموذج
   بدل واحد) — وهي الطريقة التي تتفوّق فقط على بيانات كبيرة وناعمة، عكس حال
   هذا المشروع تماماً.
3. **المشكلة الفعلية ليست في عدم اليقين، بل في الإشارة نفسها.** ~230 تجربة
   عبر `signal_discovery_lab` (مرشّحات قياسية + SHAP + Matrix Profile +
   عنقدة + اختبار عابر للأصول) لم تجد إشارة خطّية/رتبية موثوقة في أي ميزة
   مفردة أو مركَّبة بسيطة. تضخيم النموذج (بايزياً أو غير ذلك) **لا يخلق
   إشارة من عدم** — لو كانت الميزات الحالية عديمة الفائدة فعلاً، فمعمارية
   أعقد ستتعلّم نفس الضجيج بثقة أعلى ظاهرياً، لا تنبؤاً أدقّ فعلياً. هذا
   بالضبط **أثره على التعميم**: مزيد من المعاملات على بيانات صغيرة بلا
   إشارة إضافية يرفع خطر الحفظ (overfitting) لا يخفّضه — التعميم يتأثّر
   سلباً، لا إيجاباً، ما لم يصاحب التضخيم دليل مسبق على وجود شيء يستحقّ
   تمثيله بمرونة أكبر.
4. **إن أُريد لاحقاً معالجة عيوب NIG المُوثَّقة تحديداً** (انكماش الدليل،
   عدم التحديد الفريد) **بدل تضخيم شامل**: البديل الأخفّ تكلفة هو
   **Variational Bayesian Last Layer (VBLL)** — بايزي في الطبقة الأخيرة
   فقط (لا الشبكة كاملة)، تكلفته الحسابية قريبة من NIG الحالي (لا مضاعفة
   N تدريبات)، وموثَّق كبديل أحدث (٢٠٢٤) بخصائص معايرة أفضل. هذا مسار أرخص
   وأكثر تحديداً من "أضف طبقة بيزية" العامة، **لكن لا يزال مشروطاً بنفس
   المنطق**: لا فائدة من معايرة أدقّ لعدم يقين حول تنبؤ لا يحمل أصلاً
   إشارة تنبّئية حقيقية.

المصدر الإضافي: [Variational Bayesian Last Layers (arXiv 2404.11599)](https://arxiv.org/pdf/2404.11599).

## الخلاصة والتوصية

**البنية المعمارية الحالية مدروسة وحديثة بشكل جيّد** — كل مكوّن تقريباً له
مبرِّر واضح ومطابق (أو أفضل من) الممارسات الموثَّقة حديثاً (RevIN، تفكيك
سببي مُطوَّر، تضمين رُقَع بـCNN مُعايَر لحجم المدخل، مسار خطّي احترازي ضدّ
فشل المحوّل، NIG مناسب للبيانات الصغيرة). **الاستثناء الوحيد** هو GQA (لا
تخطئ، لكن بلا الفائدة التي صُمِّمت لأجلها في هذا السياق تحديداً) — تعقيد
غير عاجل التبسيط لا معطِّل للصحّة.

**لا تُنصَح إضافة طبقة بيزية أو تضخيم النموذج الآن.** ليس لأن الفكرة سيّئة
مطلقاً، بل لأن ترتيب الأولوية الصحيح معكوس: أولاً إثبات وجود إشارة تستحق
معمارية أكبر (لم يحدث بعد رغم ~230 تجربة)، ثم إن ثبت ذلك، الأولوية للبيانات
(أصول إضافية غير مترابطة، ميزات جديدة كـfunding rate/OI) قبل تعقيد
المعمارية — تعقيد يتعلّم من إشارة موجودة لا يخترعها. القرار قابل لإعادة
النظر فوراً إن ظهر دليل مستقبلي (تجربة مقبولة فعلاً في `signal_discovery_lab`
أو `signal_evaluation_axis`) يستحقّ تمثيلاً أغنى.
